<h1>index (swipe a gauche ou a droite)</h1>
<div class="d-flex flex-column align-items-center" data-controller="swipe">
  <div class="empty-user d-none" >
    <%= image_tag("simba.webp", height: 300, width: 300) %>
    <h2>Plus d'autres utilisateurs. Il n'y a plus qu'à voir si les planètes s'alignent...</h2>
  </div>
  <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">

      <% @users.each do |user| %>
        <div class="carousel-item" >

          <div class="d-flex flex-column" data-swipe-target="user" data-current-user-id="<%= current_user.id %>" data-mate-id="<%= user.id %>">

            <%= cl_image_tag user.photos.first.key, height: 300, width: 300, crop: :fill %>

            <div>
              <h2><%= user.username %></h2>
            </div>

          </div>
        </div>
      <% end %>

    </div>
  </div>

  <div class=" d-flex justify-content-between match-buttons">
    <div class="btn btn-danger" data-action="click->swipe#swipeLeft">X</div>
    <div class="btn btn-success" data-action="click->swipe#swipeRight">O</div>
  </div>
</div>
