<div class="detailed-profile-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @mate.photos.first.key, height: 450, crop: :fill %>')">
</div>

<div class="container detailed-profile-content">
  <div class="py-3">
    <div class="d-flex justify-content-between">
      <h1>
        <strong><%= @mate.username %></strong>,
        <span><%= ((Date.today - @mate.birth_date)/365).floor %> ans</span>
      </h1>

      <h1>
        <%= eval(current_user.affinity_scores)[params[:id].to_i] %>%
      </h1>
    </div>

    <div class="d-flex border-bottom pb-3">
      <span class="me-1">☉</i></span>
      <span class="me-2"><%= @mate.sign.capitalize %></span>

      <span class="me-1">↑</span>
      <span class="me-2"><%= @mate.rising.capitalize %></span>

      <span class="me-1">☽</span>
      <span class="me-2"><%= @mate.moon.capitalize %></span>
    </div>

    <div class="pt-2 border-bottom">
      <h5 class="mb-1">
        A propos de moi
      </h5>
      <p>
        <%= @mate.description %>
      </p>
    </div>

    <div class="pt-2 border-bottom">
      <h5 class="mb-1">
        Mes passions
      </h5>
      <p>
        <%= @mate.hobbies %>
      </p>
    </div>

    <div class="pt-2 border-bottom">
      <h5 class="mb-1">
        Ma relation avec <%= @mate.username %> vue par les planètes...
      </h5>
      <% eval(current_user.love_compatibility_reports)[params[:id].to_i].each do |element| %>
        <p>
          <%= element %>
        </p>
      <% end %>
    </div>
  </div>
</div>


<%# <div class="card-carousel" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @mate.photos.first.key, height: 450, crop: :fill %>
<%# </div> %>

<div>
  <%= render "navbar/navbar"%>
</div>
